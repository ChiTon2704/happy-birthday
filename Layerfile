# Sử dụng Node.js làm base image
FROM node:14

# Thiết lập thư mục làm việc
WORKDIR /app

# Copy file package.json và package-lock.json (nếu có)
COPY package*.json ./

# Cài đặt các phụ thuộc
RUN npm install

# Copy toàn bộ mã nguồn vào container
COPY . .

# Build ứng dụng Vite.js
RUN npm run build

# Cài đặt một server tĩnh để phục vụ các file build (sử dụng serve)
RUN npm install -g serve

# Khởi động server để phục vụ ứng dụng
CMD ["serve", "-s", "dist"]